def solution(num_list):
	  count = 0
    for i in num_list:
        while i != 0:
						count += 1
						if i % 2 == 0:
								i /= 2
		        else:
								i = (i - 1) / 2
								# i //= 2
		return count